<div class="container">
  <% if(auth.isAutheticeted){ %>
    <div id="app">
      
    </div>
  <% } else { %>
    <h1><%= title %></h1>
    <p><%= message %></p>
    <a 
    class="waves-effect waves-light btn" 
    href="/login">Login<a/>
  <% } %>
</div>
